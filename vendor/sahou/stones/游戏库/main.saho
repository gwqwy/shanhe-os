# 游戏库 —— 纯 sahou 实现的游戏随机工具（依赖内置 随机 模块的原语）

# 掷骰子：一个 面数 面的骰子（默认 6 面），返回 1 到 面数
函数 掷骰子(面数)
    返回 随机.数(1, 面数)
完毕

# 洗牌：返回打乱后的新列表（Fisher-Yates，不改原列表）
函数 洗牌(原列表)
    结果 = 列表(原列表)
    末 = 长度(结果) - 1
    当 末 > 0
        抽中 = 随机.数(0, 末)
        暂 = 结果[末]
        结果[末] = 结果[抽中]
        结果[抽中] = 暂
        末 = 末 - 1
    完毕
    返回 结果
完毕

# 抽奖：从名单里不放回地抽出 个数 个，返回新列表；个数超出名单长度报错
函数 抽奖(名单, 个数)
    如果 个数 > 长度(名单)
        抛出("名单只有 {长度(名单)} 个人，抽不出 {个数} 个。")
    完毕
    池 = 洗牌(名单)
    返回 取(池, 0, 个数)
完毕

# 抛硬币：返回 "正" 或 "反"
函数 抛硬币()
    如果 随机.数(0, 1) == 0
        返回 "正"
    完毕
    返回 "反"
完毕
