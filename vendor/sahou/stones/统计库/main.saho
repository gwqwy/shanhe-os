# 统计库 —— 纯 sahou 实现的基础统计量（v2 标准库 stones 包）
# 只依赖核心内置函数，宿主与浏览器（经 sahou build）都能用。
# 口径说明：方差/标准差按"总体口径"（除以 n）计算，适合入门教学场景。

# 平均值：总和除以个数；空列表报错
函数 平均值(数列表)
    如果 长度(数列表) == 0
        抛出("平均值需要对非空列表计算。")
    完毕
    返回 求和(数列表) / 长度(数列表)
完毕

# 中位数：排序后取中间；偶数个取中间两数的平均
函数 中位数(数列表)
    个 = 长度(数列表)
    如果 个 == 0
        抛出("中位数需要对非空列表计算。")
    完毕
    排好 = 排序(数列表)
    如果 个 % 2 == 1
        返回 排好[(个 - 1) / 2]
    完毕
    返回 (排好[个 / 2 - 1] + 排好[个 / 2]) / 2
完毕

# 众数：出现次数最多的值；并列时返回先达到最高次数的那个
函数 众数(数列表)
    如果 长度(数列表) == 0
        抛出("众数需要对非空列表计算。")
    完毕
    计数 = {}
    最多次 = 0
    最常见 = 空值
    for x 于 数列表
        如果 包含(计数, x)
            计数[x] = 计数[x] + 1
        否则
            计数[x] = 1
        完毕
        如果 计数[x] > 最多次
            最多次 = 计数[x]
            最常见 = x
        完毕
    完毕
    返回 最常见
完毕

# 极差：最大值减最小值
函数 极差(数列表)
    如果 长度(数列表) == 0
        抛出("极差需要对非空列表计算。")
    完毕
    返回 最大值(数列表) - 最小值(数列表)
完毕

# 方差：每个数与平均值之差的平方，再取平均（总体口径，除以 n）
函数 方差(数列表)
    个 = 长度(数列表)
    如果 个 == 0
        抛出("方差需要对非空列表计算。")
    完毕
    均 = 平均值(数列表)
    总 = 0
    for x 于 数列表
        总 = 总 + (x - 均) * (x - 均)
    完毕
    返回 总 / 个
完毕

# 标准差：方差的平方根（开方用内置 平方根）
函数 标准差(数列表)
    返回 平方根(方差(数列表))
完毕
