# 算术库 —— 纯 sahou 实现的整数算法（v2 标准库 stones 包）
# 只依赖核心内置函数，宿主与浏览器（经 sahou build）都能用。

# 最大公约数：辗转相除法；负数按绝对值处理
函数 最大公约数(a, b)
    如果 a < 0
        a = -a
    完毕
    如果 b < 0
        b = -b
    完毕
    当 b != 0
        余 = a % b
        a = b
        b = 余
    完毕
    返回 a
完毕

# 最小公倍数：两数都不为 0 时按公式；任一为 0 得 0
函数 最小公倍数(a, b)
    如果 a == 0 或者 b == 0
        返回 0
    完毕
    公 = 最大公约数(a, b)
    返回 (a * b) / 公
完毕

# 约分：把分数化为最简，符号归到分子；返回 [分子, 分母]
函数 约分(分子, 分母)
    如果 分母 == 0
        抛出("分母不能是 0。")
    完毕
    符号 = 1
    如果 分母 < 0
        符号 = -符号
        分母 = -分母
    完毕
    如果 分子 < 0
        符号 = -符号
        分子 = -分子
    完毕
    公 = 最大公约数(分子, 分母)
    如果 公 == 0
        返回 [0, 分母]
    完毕
    返回 [符号 * (分子 / 公), 分母 / 公]
完毕

# 是否质数：试除到 平方根
函数 是否质数(n)
    如果 n != 取整数值(n)
        返回 假
    完毕
    如果 n < 2
        返回 假
    完毕
    如果 n < 4
        返回 真
    完毕
    如果 n % 2 == 0
        返回 假
    完毕
    除数 = 3
    当 除数 * 除数 <= n
        如果 n % 除数 == 0
            返回 假
        完毕
        除数 = 除数 + 2
    完毕
    返回 真
完毕

# 质因数分解：返回因子列表（从小到大）；1 与 0、负数返回空列表
函数 质因数分解(n)
    因子 = []
    如果 n < 2
        返回 因子
    完毕
    当 n % 2 == 0
        因子.添加(2)
        n = n / 2
    完毕
    除数 = 3
    当 除数 * 除数 <= n
        当 n % 除数 == 0
            因子.添加(除数)
            n = n / 除数
        完毕
        除数 = 除数 + 2
    完毕
    如果 n > 1
        因子.添加(n)
    完毕
    返回 因子
完毕

# 取整数值：小数的整数部分（向零取整）
函数 取整数值(x)
    段 = 分割(文本(x), ".")
    如果 长度(段) == 1
        返回 x
    完毕
    返回 数(段[0])
完毕

# 阶乘：n! = 1×2×…×n；负数报错
函数 阶乘(n)
    如果 n < 0
        抛出("负数没有阶乘。")
    完毕
    结果 = 1
    for i 于 从a到b(2, n + 1)
        结果 = 结果 * i
    完毕
    返回 结果
完毕
