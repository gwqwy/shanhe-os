# 集合运算 —— 基于列表的交集/并集/差集（去重、保序）
# 与 文本库/游戏库 同为纯 sahou 包，宿主与浏览器（sahou build）都可用

# 交集：既在 a 又在 b 的元素（去重、按 a 的顺序）
函数 交集(a, b)
    结果 = []
    for x 于 a
        如果 包含(b, x) 并且 非 包含(结果, x)
            结果.添加(x)
        完毕
    完毕
    返回 结果
完毕

# 并集：a 与 b 的全部元素（去重、先 a 后 b）
函数 并集(a, b)
    结果 = []
    for x 于 a
        如果 非 包含(结果, x)
            结果.添加(x)
        完毕
    完毕
    for x 于 b
        如果 非 包含(结果, x)
            结果.添加(x)
        完毕
    完毕
    返回 结果
完毕

# 差集：在 a 里但不在 b 里的元素（去重、按 a 的顺序）
函数 差集(a, b)
    结果 = []
    for x 于 a
        如果 非 包含(b, x) 并且 非 包含(结果, x)
            结果.添加(x)
        完毕
    完毕
    返回 结果
完毕

# 对称差：只在 a 或只在 b 的元素（并集减去交集）
函数 对称差(a, b)
    返回 并集(差集(a, b), 差集(b, a))
完毕

# 是否子集：a 的每个元素都在 b 里吗（空列表是任何列表的子集）
函数 是否子集(a, b)
    for x 于 a
        如果 非 包含(b, x)
            返回 假
        完毕
    完毕
    返回 真
完毕
