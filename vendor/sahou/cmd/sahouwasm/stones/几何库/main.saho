# 几何库 —— 纯 sahou 实现的基础几何计算（v2 标准库 stones 包）
# 圆周率取自内置 数学 模块（宿主与浏览器双侧同语义）。
# 半径/边长为负会抛错；三边不成三角形也会抛错并说明原因。

# 圆面积：圆面积(2) ≈ 12.5663706143592
函数 圆面积(半径)
    如果 半径 < 0
        抛出("半径不能是负数，收到的是 {半径}。")
    完毕
    返回 数学.圆周率 * 半径 * 半径
完毕

# 圆周长：圆周长(2) ≈ 12.5663706143592
函数 圆周长(半径)
    如果 半径 < 0
        抛出("半径不能是负数，收到的是 {半径}。")
    完毕
    返回 2 * 数学.圆周率 * 半径
完毕

# 矩形面积
函数 矩形面积(长, 宽)
    如果 长 < 0 或者 宽 < 0
        抛出("长和宽不能是负数。")
    完毕
    返回 长 * 宽
完毕

# 三角形面积：底 × 高 ÷ 2
函数 三角形面积(底, 高)
    如果 底 < 0 或者 高 < 0
        抛出("底和高不能是负数。")
    完毕
    返回 底 * 高 / 2
完毕

# 梯形面积：(上底 + 下底) × 高 ÷ 2
函数 梯形面积(上底, 下底, 高)
    如果 上底 < 0 或者 下底 < 0 或者 高 < 0
        抛出("梯形的边和高不能是负数。")
    完毕
    返回 (上底 + 下底) * 高 / 2
完毕

# 勾股斜边：直角两条直角边求斜边。勾股斜边(3, 4) 得 5
函数 勾股斜边(直角边甲, 直角边乙)
    如果 直角边甲 < 0 或者 直角边乙 < 0
        抛出("直角边不能是负数。")
    完毕
    返回 平方根(直角边甲 * 直角边甲 + 直角边乙 * 直角边乙)
完毕

# 两点距离：平面直角坐标系里两点之间的线段长
函数 两点距离(x1, y1, x2, y2)
    返回 勾股斜边(x2 - x1, y2 - y1)
完毕

# 海伦面积：已知三边求三角形面积。海伦面积(3, 4, 5) 得 6
函数 海伦面积(a, b, c)
    如果 a <= 0 或者 b <= 0 或者 c <= 0
        抛出("三边长都必须是正数。")
    完毕
    如果 a + b <= c 或者 a + c <= b 或者 b + c <= a
        抛出("边长 {a}、{b}、{c} 拼不成三角形（两边之和要大于第三边）。")
    完毕
    半周 = (a + b + c) / 2
    返回 平方根(半周 * (半周 - a) * (半周 - b) * (半周 - c))
完毕
