# 卅· 猜数字游戏
# 运行：sahou run 猜数字.saho
#
# 说明一：v1 没有 break，所以 "当 真" 循环用手动布尔标志 "继续猜" 控制退出。
# 说明二：为了不依赖键盘输入（方便自动测试），猜测写成一个列表逐一演示，
#         而不是调用 输入()。最后一轮 97 猜中。

# ── 1. 出题：秘密数字 + 候选范围 ──────────────────────────
秘密数字 = 97
候选数字 = 从a到b(1, 100)          # [1, 2, ..., 99]（含 1、不含 100）

print("我想好了一个 1 到 99 之间的数字，你来猜。")

# 用 包含() 检查秘密数字确实落在候选范围里，避免题目本身出错
如果 包含(候选数字, 秘密数字)
    print("提示：答案藏在 {长度(候选数字)} 个候选数字之中。")
否则
    print("题目出错了：{秘密数字} 不在候选范围里。")
    返回
完毕

# ── 2. 判定函数：把 "猜中 / 小了 / 大了" 收进一个函数 ────────
函数 判定(猜测)
    如果 猜测 == 秘密数字
        返回 "猜中"
    又如 猜测 < 秘密数字
        返回 "小了"
    否则
        返回 "大了"
    完毕
完毕

# ── 3. 模拟的几组猜测（最后一轮猜中 97）──────────────────
猜测列表 = [50, 75, 88, 94, 97]
总轮数 = 长度(猜测列表)

次数 = 0
猜中 = 假
轮次 = 0
继续猜 = 真                        # 布尔标志：v1 用它代替 break

当 继续猜
    如果 轮次 >= 总轮数
        继续猜 = 假                # 手动退出①：猜测已经用完
    否则
        猜测 = 猜测列表[轮次]
        轮次 = 轮次 + 1
        次数 = 次数 + 1

        如果 非 包含(候选数字, 猜测)
            print("第 {次数} 次：{猜测} 不在 1 到 99 之间，跳过这一轮。")
        否则
            print("第 {次数} 次猜测：{猜测}")
            结果 = 判定(猜测)
            如果 结果 == "猜中"
                print("  → 猜中了！")
                猜中 = 真
                继续猜 = 假        # 手动退出②：猜中了就停
            又如 结果 == "大了"
                print("  → 大了，往小一点猜。")
            否则
                print("  → 小了，往大一点猜。")
            完毕
        完毕
    完毕
完毕

# ── 4. 统计与结果 ────────────────────────────────────────
如果 猜中
    print("congratulation! 你猜中了，答案就是 {秘密数字}，一共猜了 {次数} 次。")
否则
    print("很遗憾，猜了 {次数} 次都没中，答案是 {秘密数字}。")
完毕
